/**
 * themes/_template/classmap.js
 * Template do classMap para um tema custom do OZI-UI.
 *
 * O classMap traduz TOKENS SEMÂNTICOS (invalid, valid, button…) para as
 * CLASSES CSS do seu framework/design system. Os componentes aplicam essas
 * classes em runtime — é como o mesmo build JS serve qualquer visual.
 *
 * Descomente, copie para themes/meu-tema/classmap.js e preencha. Carregue o
 * arquivo APÓS o ozi.js. Como alternativa a este arquivo, você pode passar o
 * classMap direto no oziConf({ theme:'custom', classMap:{...} }).
 */
/*
(function (window) {
    'use strict';

    if (!window.oziConf) {
        console.warn('[OZI] classmap.js: oziConf não encontrado (carregue após ozi.js).');
        return;
    }

    window.oziConf({
        classMap: {
            // — validação —
            invalid:        '',   // classe para campo inválido
            valid:          '',   // classe para campo válido
            formValidated:  '',   // classe no <form> após validar
            feedback:       '',   // classe da mensagem de erro

            // — botões —
            button:          '',  // botão padrão
            buttonPrimary:   '',
            buttonSecondary: '',
            buttonDanger:    '',

            // — utilitários —
            hidden:   '',         // esconde elemento
            loading:  '',         // estado de carregamento
            disabled: '',         // elemento desabilitado
            active:   '',         // elemento ativo
            badge:    ''          // badge / pill
        }
    });

})(window);
*/
